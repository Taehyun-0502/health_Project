<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.health.app.member.MemberMapper">

    <!-- 회원 가입 SQL -->
  <insert id="join" parameterType="MemberDTO">
    insert into "h_member" (username, password, name, email, role, gym_id, birth)
    values(#{username}, #{password}, #{name}, #{email, jdbcType=VARCHAR}, #{role}, #{gymId}, #{birth})
    </insert>  


  <!-- 아이디(전화번호) 중복확인 SQL -->
  <select id="idcheck" parameterType="MemberDTO" resultType="MemberDTO">
  select * from "h_member" where username = #{username}
  </select>

  <update id="update" parameterType="MemberDTO">
  update h_member
  set
  password = #{password},
  email = #{email}
  where username = #{username}
  </update>

  <!-- 권한(role) 기준 회원 목록 조회 SQL (대소문자 무관 비교) -->
  <select id="findByRole" parameterType="String" resultType="MemberDTO">
  select * from "h_member" where upper(role) = upper(#{role})
  </select>

  <!-- gym_id 기준 사장님(OWNER) 계정 단건 조회 SQL - 알림 발송 대상(username) 식별용 -->
  <select id="findOwnerByGymId" parameterType="Long" resultType="MemberDTO">
  select * from "h_member" where gym_id = #{gymId} and upper(role) = 'OWNER' limit 1
  </select>

  <!-- gym_id 기준 지점별 소속 일반 회원 목록 조회 SQL -->
  <select id="findMembersByGymId" parameterType="Long" resultType="MemberDTO">
  select username, name, gym_id, role from "h_member" where gym_id = #{gymId} and upper(role) = 'MEMBER' order by name asc
  </select>

  <insert id="updateToken">
  insert into "h_refresh_token"
  values(#{username},#{refreshToken},#{expiryDate})
  on conflict (username)
  do update set refresh_token = #{refreshToken}, expiry_date = #{expiryDate}
  </insert>

  <!-- 1. 리프레쉬 토큰 대조 조회 쿼리 -->
<select id="getRefreshToken" parameterType="String" resultType="RefreshTokenDTO">
    SELECT username, refresh_token as refreshToken, expiry_date as expiryDate
    FROM "h_refresh_token"
    WHERE refresh_token = #{refreshToken}
</select>

<!-- 2. 로그아웃 시 토큰 영구 소거 쿼리 -->
<delete id="deleteToken" parameterType="Long">
    DELETE FROM "h_refresh_token"
    WHERE username = #{username}
</delete>

</mapper>
