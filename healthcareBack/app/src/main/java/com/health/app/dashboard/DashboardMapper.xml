<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.health.app.dashboard.DashboardMapper">

    <!-- ===== 위젯 설정 (h_dashboard_widget) ===== -->

    <!-- 로그인 사용자의 위젯 설정 목록 조회 SQL -->
    <select id="widgetList" parameterType="Long" resultType="DashboardDTO">
    select widget_id as widgetId,
           username,
           gym_id as gymId,
           role,
           widget_key as widgetKey,
           is_active as isActive,
           has_data as hasData,
           sort_order as sortOrder,
           updated_at as updatedAt
    from "h_dashboard_widget"
    where username = #{username}
    order by sort_order asc, widget_id asc
    </select>

    <!-- 역할별 기본 위젯 설정 등록 SQL
         (widgets/data 동시 호출로 최초 초기화가 겹쳐도 유니크 충돌 없이 무시) -->
    <insert id="widgetAdd" parameterType="DashboardDTO">
    insert into "h_dashboard_widget"
    (username, gym_id, role, widget_key, is_active, has_data, sort_order, updated_at)
    values
    (#{username}, #{gymId, jdbcType=BIGINT}, #{role}, #{widgetKey}, #{isActive}, #{hasData}, #{sortOrder}, now())
    on conflict (username, widget_key) do nothing
    </insert>

    <!-- 위젯 표시 여부 토글 SQL (데이터 없는 위젯은 켤 수 없음) -->
    <update id="widgetToggle" parameterType="DashboardDTO">
    update "h_dashboard_widget"
    set is_active = #{isActive},
        updated_at = now()
    where username = #{username}
      and widget_key = #{widgetKey}
      and (has_data = true or #{isActive} = false)
    </update>

    <!-- 위젯 표시 순서 변경 SQL -->
    <update id="widgetOrderUpdate" parameterType="DashboardDTO">
    update "h_dashboard_widget"
    set sort_order = #{sortOrder},
        updated_at = now()
    where username = #{username}
      and widget_key = #{widgetKey}
    </update>

    <!-- 위젯 데이터 적재 여부 갱신 SQL
         (표시 여부는 서비스에서 판정: 최초 진입 시 데이터 있으면 켜짐, 이후 사용자 설정 유지, 데이터 소실 시 잠금) -->
    <update id="widgetHasDataUpdate" parameterType="DashboardDTO">
    update "h_dashboard_widget"
    set has_data = #{hasData},
        is_active = #{isActive},
        updated_at = now()
    where username = #{username}
      and widget_key = #{widgetKey}
    </update>

    <!-- 로그인 사용자의 소속 지점 번호 조회 SQL -->
    <select id="memberGymId" parameterType="Long" resultType="Long">
    select gym_id from "h_member"
    where username = #{username}
    </select>

    <!-- ===== 위젯 데이터 (역할별 집계) ===== -->

    <!-- 관계사: 활성 제휴 계약(contract=1) 기준 계약 체육관 수 + 이번 달 신규 집계 SQL -->
    <select id="adminGymCount" resultType="map">
    select count(distinct d.gym_id) as "total",
           count(distinct case when d.signed_at >= date_trunc('month', current_date) then d.gym_id end) as "newThisMonth"
    from "h_contract_data" d
    where d.contract = 1
      and d.status = 'SIGNED'
      and (d.end_date is null or d.end_date >= current_date)
    </select>

    <!-- 관계사: 30일 내 구독(제휴 계약) 만료 체육관 목록 조회 SQL -->
    <select id="adminExpiringSubscription" resultType="map">
    select g.gym_name as "name",
           d.end_date as "endDate",
           (d.end_date - current_date) as "dday"
    from "h_contract_data" d
    left join "h_gym" g on d.gym_id = g.gym_id
    where d.contract = 1
      and d.status = 'SIGNED'
      and d.end_date between current_date and current_date + 30
    order by d.end_date asc
    </select>

    <!-- 관계사: 설문(h_survey) 4개 항목(서비스/기구/직원/가격) 평점 기반 체육관 만족도 집계 SQL
         서비스·기구·직원은 세부 2문항(rate1/rate2)의 평균을 항목 점수로 사용하되, 구설문 데이터처럼 한쪽이 NULL이면
         있는 값만 사용(둘 다 NULL이면 해당 행은 그 항목 평균 계산에서만 제외, avg()가 NULL을 자동 무시).
         가격은 단일 문항(cost_rate)을 그대로 사용. 항목별 평균을 먼저 구한 뒤 4개 항목 평균을 최종 점수로 산출해
         특정 행의 한 항목 결측이 다른 항목 점수까지 통째로 깎지 않도록 함 -->
    <select id="adminNpsSummary" resultType="map">
    select count(*) as "total",
           coalesce(round((
             avg(case
                   when s.service_rate1 is null and s.service_rate2 is null then null
                   when s.service_rate1 is null then s.service_rate2::numeric
                   when s.service_rate2 is null then s.service_rate1::numeric
                   else (s.service_rate1 + s.service_rate2) / 2.0
                 end)
             + avg(case
                   when s.equip_rate1 is null and s.equip_rate2 is null then null
                   when s.equip_rate1 is null then s.equip_rate2::numeric
                   when s.equip_rate2 is null then s.equip_rate1::numeric
                   else (s.equip_rate1 + s.equip_rate2) / 2.0
                 end)
             + avg(case
                   when s.employee_rate1 is null and s.employee_rate2 is null then null
                   when s.employee_rate1 is null then s.employee_rate2::numeric
                   when s.employee_rate2 is null then s.employee_rate1::numeric
                   else (s.employee_rate1 + s.employee_rate2) / 2.0
                 end)
             + avg(s.cost_rate)
           ) / 4.0, 1), 0) as "averageScore"
    from "h_survey" s
    </select>

    <!-- 공용: 최근 6개월 월별 매출 집계 SQL (gymId 없으면 전체 지점 합산) -->
    <select id="monthlyRevenue" parameterType="DashboardDTO" resultType="map">
    select to_char(p.pay_date, 'YYYY-MM') as "month",
           sum(p.pay_price) as "total"
    from "h_payment" p
    where p.pay_date >= date_trunc('month', current_date) - interval '5 months'
    <if test="gymId != null">
      and p.gym_id = #{gymId}
    </if>
    group by to_char(p.pay_date, 'YYYY-MM')
    order by "month" asc
    </select>

    <!-- 공용: 최근 6개월 월별 지출 집계 SQL (gymId 없으면 전체 지점 합산) -->
    <select id="monthlyExpense" parameterType="DashboardDTO" resultType="map">
    select to_char(e.expense_date, 'YYYY-MM') as "month",
           sum(e.expense_price) as "total"
    from "h_expense" e
    where e.expense_date >= date_trunc('month', current_date) - interval '5 months'
    <if test="gymId != null">
      and e.gym_id = #{gymId}
    </if>
    group by to_char(e.expense_date, 'YYYY-MM')
    order by "month" asc
    </select>

    <!-- 사장님(대시보드 2026-07-22 개편): 유효 회원 계약(contract=3,4,5 / ACTIVE만) 기준 distinct 수신자 수
         + 유형 구성(이용권/PT/PT체험) + 이번 달 신규 집계 SQL -->
    <select id="ownerActiveMemberCount" parameterType="Long" resultType="map">
    select count(distinct d.receiver_id) as "total",
           count(distinct case when d.contract = 3 then d.receiver_id end) as "membership",
           count(distinct case when d.contract = 4 then d.receiver_id end) as "pt",
           count(distinct case when d.contract = 5 then d.receiver_id end) as "trial",
           count(distinct case when d.signed_at >= date_trunc('month', current_date) then d.receiver_id end) as "newThisMonth"
    from "h_contract_data" d
    where d.gym_id = #{gymId}
      and d.contract in (3, 4, 5)
      and d.status = 'ACTIVE'
    </select>

    <!-- 사장님: 30일 내 계약 만료 회원 목록 조회 SQL -->
    <select id="ownerExpiringContract" parameterType="Long" resultType="map">
    select d.receiver_name as "name",
           d.end_date as "endDate",
           (d.end_date - current_date) as "dday"
    from "h_contract_data" d
    where d.gym_id = #{gymId}
      and d.contract in (3, 4, 5)
      and d.status in ('SIGNED', 'ACTIVE')
      and d.end_date between current_date and current_date + 30
    order by d.end_date asc
    </select>

    <!-- 사장님: 지점 회원 이탈 예측(h_churn_result, healthModel 일일 배치 적재분) 집계 SQL
         (임시 유지 - model 팀원 전달 후 교체 예정)
         (h_churn_result는 이력 누적 가능 - 회원별 최신 1건만 집계, h_member 조인으로 지점 격리) -->
    <select id="ownerChurnSummary" parameterType="Long" resultType="map">
    select count(*) as "total",
           coalesce(round(avg(r.churn_rate), 3), 0) as "averageChurnRate",
           -- 위험군 = 이탈률 45% 이상 (개입 + 위험 등급 합계)
           count(case when r.churn_rate >= 0.45 then 1 end) as "highRiskCount",
           count(case when r.churn_rate >= 0.7 then 1 end) as "urgentCount",
           count(case when r.churn_rate >= 0.45 and r.churn_rate &lt; 0.7 then 1 end) as "interveneCount",
           count(case when r.churn_rate >= 0.25 and r.churn_rate &lt; 0.45 then 1 end) as "watchCount",
           count(case when r.churn_rate &lt; 0.25 then 1 end) as "stableCount"
    from (
        select distinct on (username) username, churn_rate
        from "h_churn_result"
        order by username, churn_date desc nulls last
    ) r
    join "h_member" mb on r.username = mb.username
    where mb.gym_id = #{gymId}
    </select>

    <!-- 트레이너: 유효 담당 PT·PT 체험 계약(contract=4,5 / SIGNED·ACTIVE) 기준 담당 회원 수 + 이번 달 신규 집계 SQL -->
    <select id="trainerMemberCount" parameterType="Long" resultType="map">
    select count(distinct d.receiver_id) as "total",
           count(distinct case when d.signed_at >= date_trunc('month', current_date) then d.receiver_id end) as "newThisMonth"
    from "h_contract_data" d
    where d.manager_id = #{username}
      and d.contract in (4, 5)
      and d.status in ('SIGNED', 'ACTIVE')
      and (d.end_date is null or d.end_date >= current_date)
    </select>

    <!-- 트레이너: 세션 소진 임박 회원 목록 조회 SQL
         (계약 세션 수량 - 계약 시작 이후 출석 횟수 = 남은 세션으로 근사 계산) -->
    <select id="trainerLowSessionMembers" parameterType="Long" resultType="map">
    select d.receiver_name as "name",
           greatest(d.quantity - (
               select count(*)
               from "h_check_inout" i
               where i.username = d.receiver_id
                 and i.check_in >= d.start_date
           ), 0) as "remain"
    from "h_contract_data" d
    where d.manager_id = #{username}
      and d.contract in (4, 5)
      and d.status in ('SIGNED', 'ACTIVE')
      and (d.end_date is null or d.end_date >= current_date)
      and d.quantity is not null
    order by "remain" asc
    limit 5
    </select>

    <!-- 트레이너: 최근 6개월 담당 회원 출석 기반 월별 세션 수행 집계 SQL -->
    <select id="trainerMonthlySession" parameterType="Long" resultType="map">
    select to_char(i.check_in, 'YYYY-MM') as "month",
           count(*) as "total"
    from "h_check_inout" i
    where i.check_in >= date_trunc('month', current_date) - interval '5 months'
      and exists (
          select 1 from "h_contract_data" d
          where d.manager_id = #{username}
            and d.contract in (4, 5)
            and d.status in ('SIGNED', 'ACTIVE')
            and (d.end_date is null or d.end_date >= current_date)
            and d.receiver_id = i.username
      )
    group by to_char(i.check_in, 'YYYY-MM')
    order by "month" asc
    </select>

    <!-- 트레이너: 담당 회원 이탈 예측(h_churn_result) 집계 SQL (회원별 최신 1건만 집계) -->
    <select id="trainerChurnSummary" parameterType="Long" resultType="map">
    select count(*) as "total",
           coalesce(round(avg(r.churn_rate), 3), 0) as "averageChurnRate",
           count(case when r.churn_rate >= 0.7 then 1 end) as "highRiskCount"
    from (
        select distinct on (username) username, churn_rate
        from "h_churn_result"
        order by username, churn_date desc nulls last
    ) r
    where exists (
        select 1 from "h_contract_data" d
        where d.manager_id = #{username}
          and d.contract in (4, 5)
          and d.status in ('SIGNED', 'ACTIVE')
          and (d.end_date is null or d.end_date >= current_date)
          and d.receiver_id = r.username
    )
    </select>

</mapper>
